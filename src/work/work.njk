{% extends 'layouts/base.njk' %}

{% block main %}
        <section class="work">
          <div class="content" id="content">
            <h2 class="content__heading">Work I've Done</h2>
            <div class="grid-6-6">
              {% for item in works %}
              <div>
                <figure class="effect">
                  <img src="{{ site.cloudinary_url }}f_auto/work/projects/{{ item.img }}" alt="{{ item.client }}" loading="lazy" width="586" height="384">
                  <figcaption>
                    <span>{{ item.name }}</span>
                    <a href="{{ item.link }}">View more</a>
                  </figcaption>
                </figure>
              </div>
              {% endfor %}
          </div>
        </section>

        <section class="companies" id="companies">
          <div class="container">
            <div class="content">
              <h2 class="content__heading">Who I've worked with</h2>
              <div class="client-list">
              {% for item in companies %}
                <div class="client">
                  <img class="client__logo" src="{{ site.cloudinary_url }}f_auto/work/clients/{{ item.img }}" alt="{{ item.alt }}" loading="lazy" width="490" height="340">
                </div>
              {% endfor %}
              </div>
            </div>
          </div>
        </section>
{% endblock %}
