--- 
title: Links
layout: page
class: links
date: 2024-04-03T14:30:00Z
intro:
  class: sml
  subheading: Sharing links to articles I've read
---

{% for month in links | reverse %}
<h2 class="month">{{ month.month }}</h2>

  {%- for link in month.links | reverse -%}
  <div class="link">
    <div class="link__details">
      <h3><a href="{{ link.url }}">{{ link.title }}</a></h3>
      {% if link.excerpt %}<p>{{ link.excerpt | safe }}</p>{% endif %}
      {# {% if link.author %}<a class="link__author u-inline-block">{{ link.author }}</a>{% endif %} #}
      <span class="link__date dt-published">Read on: {{ link.date }}</span>
    </div>
  </div>
  {%- endfor -%}
{% endfor %}
