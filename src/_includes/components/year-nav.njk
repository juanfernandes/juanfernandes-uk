{% if years and years.length %}
  <div class="media-years">
    <span>Years:</span>
    {% for y in years %}
      <a href="{{ base }}/year/{{ y }}/">{{ y }}</a>{% if not loop.last %} · {% endif %}
    {% endfor %}
  </div>
{% endif %}
